import React from "react";
import { View, Text } from "react-native";

// Components 

import Button from "../Button";



const Categories = () => {
    return (
        <View>
            <Button
                onPress={null} children="beff"
            />
            <Button
                onPress={null} children="beff"
            />
            <Button
                onPress={null} children="Yemek kategorisi"
            />
            <Button
                onPress={null} children="Yemek kategorisi aa"
            />
        </View>
    );
};


export default Categories;